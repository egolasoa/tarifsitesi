// Tarif Ekleme Formu Gönderildiğinde
document.getElementById('tarif-ekle-form').addEventListener('submit', function (e) {
    e.preventDefault();
    // Tarif ekleme işlemleri burada yapılabilir
});
